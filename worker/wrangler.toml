name = "weekly-planner-reminders"
main = "reminders.mjs"
compatibility_date = "2024-09-23"
workers_dev = false

# Runs every 5 minutes to send due reminders.
[triggers]
crons = ["*/5 * * * *"]

# Same D1 database as the app.
[[d1_databases]]
binding = "DB"
database_name = "weekly_planner_db"
database_id = "76809ded-129c-45de-a3b4-d78bb00c590e"
migrations_dir = "../migrations"

[vars]
VAPID_PUBLIC_KEY = "BLmfmOo3jSoQX4Ra8y2_FS6AmtfummWRY8zjuhhUqslkT_vkbFGVOYeyjebiOmxGgV1BsnrYS_sAlykocosViAE"
VAPID_SUBJECT = "mailto:muhammadrafay588@gmail.com"

# Secret (set with: wrangler secret put VAPID_PRIVATE_JWK --config worker/wrangler.toml):
#   VAPID_PRIVATE_JWK = '{"kty":"EC","crv":"P-256","x":"...","y":"...","d":"..."}'
